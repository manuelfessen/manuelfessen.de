---
import dayjs from 'dayjs'

export interface Props {
  reading: number;
}

const { title, date, keywords, reading } = Astro.props;
---

<div class="flex justify-between mb-4 mt-8 text-xs no-decoration">
  <a href="/" class="">← Return home</a>
  <span class="">{keywords}</span> 
</div>


<article class="space-y-1 prose max-w-none prose-list-disc">

  <!-- Headline -->
  <h1 class="font-normal leading-tight text-3xl">{title}</h1>

  <!-- Date -->
  <p class="text-xs">Posted on {dayjs(date).format('DD.MM.YYYY')} · 📖 {reading.text}ing time</p>

  <div class="divider"></div> 

  <!-- Content -->
  <section class="mx-auto pb-6 lg:pb-12 content">
    <slot />
  </section>
</article>